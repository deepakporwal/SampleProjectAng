import { MyhihlihtDirective } from './myhihliht.directive';

describe('MyhihlihtDirective', () => {
  it('should create an instance', () => {
    const directive = new MyhihlihtDirective();
    expect(directive).toBeTruthy();
  });
});
